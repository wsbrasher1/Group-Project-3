import React from "react";

const Navbar = props => (
    <navbar bg="dark">
        <Navbar.Brand href="#home">Craft Catcher</Navbar.Brand>
        <Navbar.Text href="#events">Events</Navbar.Text>
    </navbar>
);

export default Navbar;